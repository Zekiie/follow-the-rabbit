import { style } from '@vanilla-extract/css';

export const flexContainer = style({
  display: 'flex',
});

export const typographyMainSmall = style({
  color: '#bdbaba',
});

export const typographyUppercase = style({
  textTransform: 'uppercase',
});

export const marginRight = style({
  marginRight: '20px',
});
